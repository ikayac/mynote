# [[マルコフモデルの係数決定ルール]]

## 呪文：「分母は『Other - Me』」

複雑な係数 $\prod \frac{\theta_m}{\theta_m - \theta_s}$ を暗記する必要はない。
以下のルールで機械的に導出できる。

### 手順
1. **主役（Me）を決める**:
   指数関数 $\exp(-\theta_s Z)$ の $\theta_s$ がその項の「主役」。
2. **分子を作る**:
   経路上のすべての $\theta$ を掛け算する。
   $$\text{Numerator} = \theta_1 \cdot \theta_2 \cdot \dots \cdot \theta_{j-1}$$
3. **分母を作る（Other - Me）**:
   経路上の「自分以外（Other）」から「自分（Me）」を引いたものを列挙して掛ける。
   $$\text{Denominator} = (\theta_1 - \theta_s)(\theta_2 - \theta_s)\dots(\theta_{last} - \theta_s)$$
   *(※ただし $\theta_s - \theta_s$ は含めない)*

### 例：状態 $s=2$ が主役のとき


# [[遷移確率導出の瞬間想起法]]

## 1. 映像イメージ：プリズム
**「直列のリレー（積）」を積分すると、光が分かれて「並列の波（和）」になる。**
- Input: $\prod$ (掛け算)
- Process: $\int$ (たたみ込み積分)
- Output: $\sum$ (部分分数分解による足し算)

## 2. 式の構築（レゴ方式）
確率 $\pi_{i,j}(Z)$ は、関わったすべての状態 $s$ ($i \dots j$) の**線形結合（足し合わせ）**である。

### 各パーツの作り方
1. **フレーム**: $\sum_{s=i}^{j} [ \dots ]$ （全員分足す）
2. **時間項**: $\exp(-\theta_s \epsilon Z)$ （主役 $\theta_s$ で減衰）
3. **係数項**:
   - 分子： $\prod \theta$ （全スピードの積）
   - 分母： $\prod (\theta_{Other} - \theta_{Me})$ （あいつ - オレ）

$$
\text{確率} = \sum_{\text{主役}=1}^{5} \left[ \frac{\text{全員の積}}{(\text{他}_1-\text{主})(\text{他}_2-\text{主})\dots} \times e^{-\text{主役}\cdot t} \right]
$$

---
date: 2026-01-0203:52
tags:
  #gemini 
  #Study
---

#  💬 チャットログ

# [[マルコフモデルの係数決定ルール（ヘヴィサイドの隠し技）]]

## ルール：「ターゲットを引き算の後ろに固定する」

遷移確率 $\pi(Z)$ は複数の「項」の足し算である。
各項の係数は、以下の機械的な操作で決定できる。暗記は不要。

### 手順プロセス
1. **ターゲット（$\theta_s$）の選定**:
   その項の指数関数 $\exp(-\theta_s Z)$ に乗っている $\theta_s$ をターゲットとする。

2. **分母の作成（引き算）**:
   経路に関わる他のすべての $\theta_m$ から、ターゲット $\theta_s$ を引く。
   $$
   \text{分母} = (\theta_1 - \mathbf{\theta_s}) (\theta_2 - \mathbf{\theta_s}) \dots (\theta_n - \mathbf{\theta_s})
   $$
   ※自分自身 $(\theta_s - \theta_s)$ のペアだけは作らずスキップする。
   ※**「常にターゲットが引く側（後ろ）」**と覚える。

3. **分子の作成**:
   経路に関わるすべての $\theta$ （遷移元の速度）を掛け合わせる。
   $$
   \text{分子} = \theta_1 \cdot \theta_2 \cdot \dots \cdot \theta_{last}
   $$

### 具体例：状態 1 $\to$ 2 $\to$ 3 の遷移（項は3つ）
**ターゲットが $\theta_2$ の項を作る場合：**
- 分子： $\theta_1 \theta_2$
- 分母： ターゲット $\theta_2$ をすべての後ろに置く。
  - $(\theta_1 - \mathbf{\theta_2})$
  - $(\theta_3 - \mathbf{\theta_2})$
  ※ $(\theta_2 - \theta_2)$ は飛ばす。

$$
\text{第2項} = \frac{\theta_1 \theta_2}{(\theta_1 - \mathbf{\theta_2})(\theta_3 - \mathbf{\theta_2})} \exp(-\mathbf{\theta_2} \epsilon Z)
$$


---
date: 2026-01-0204:01
tags:
  #gemini 
  #Study
---

#  💬 チャットログ



# [[遷移確率 π(1→5) の完全構築フロー]]

複雑な公式を暗記するのではなく、**「3つの層（レイヤー）を積み上げて式を完成させる」**手順として覚える。

## 完成形のイメージ
$$
\pi_{1,5}(Z) = \text{[項1]} + \text{[項2]} + \text{[項3]} + \text{[項4]} + \text{[項5]}
$$
5つの状態（1, 2, 3, 4, 5）が関わるので、5つのパーツの足し算になる。

---

## 構築の3ステップ

### Step 1: 「主役（指数関数）」を配置する
まず、5つの項の「骨格」を作る。
各項には、必ず1人の**ターゲット（主役となる $\theta$）**がいる。
$s=1$ から $5$ まで、順番に並べる。

$$
\dots \exp(-\mathbf{\theta_1} \epsilon Z) + \dots \exp(-\mathbf{\theta_2} \epsilon Z) + \dots + \dots \exp(-\mathbf{\theta_5} \epsilon Z)
$$

> [!CHECK]
> ここで、それぞれの項の「ターゲット」が決まる。
> - 第1項のターゲット： $\theta_1$
> - 第3項のターゲット： $\theta_3$

### Step 2: 「共通の分子」を乗せる
係数の**分子（上の部分）は、すべての項で「完全に同じ」**である。
何も考えずに、遷移に関わった**「出口のスピード」**を全部掛け算して乗せる。

- 経路： $1 \to 2 \to 3 \to 4 \to 5$
- 出口： $\theta_1, \theta_2, \theta_3, \theta_4$ （※5はゴールなので出口ではない）

$$
\text{分子} = \theta_1 \cdot \theta_2 \cdot \theta_3 \cdot \theta_4
$$
これを全項の分子に置く。

### Step 3: 「固有の分母」を作る（引き算ルール）
ここだけが項ごとに違う。
**「ターゲットを後ろに置いて引く」**というルールで分母を埋める。

**例：第3項（ターゲット $\theta_3$）を作る場合**
登場人物全員（1, 2, 3, 4, 5）から、ターゲット（3）を引き算する。
$$
\text{分母} = (\theta_1 - \mathbf{\theta_3})(\theta_2 - \mathbf{\theta_3})(\text{スキップ})(\theta_4 - \mathbf{\theta_3})(\theta_5 - \mathbf{\theta_3})
$$
※自分自身 $(\theta_3 - \theta_3)$ はゼロになるので書かない（スキップ）。

---

## 実践：式の書き下し（完成形）

以上の3ステップを組み合わせると、自動的に以下の式が出来上がる。

$$
\begin{aligned}
\pi_{1,5}(Z) &= \frac{\theta_1 \theta_2 \theta_3 \theta_4}{(\theta_2-\mathbf{\theta_1})(\theta_3-\mathbf{\theta_1})(\theta_4-\mathbf{\theta_1})(\theta_5-\mathbf{\theta_1})} e^{-\mathbf{\theta_1} \epsilon Z} \
&+ \frac{\theta_1 \theta_2 \theta_3 \theta_4}{(\theta_1-\mathbf{\theta_2})(\theta_3-\mathbf{\theta_2})(\theta_4-\mathbf{\theta_2})(\theta_5-\mathbf{\theta_2})} e^{-\mathbf{\theta_2} \epsilon Z} \
&+ \frac{\theta_1 \theta_2 \theta_3 \theta_4}{(\theta_1-\mathbf{\theta_3})(\theta_2-\mathbf{\theta_3})(\theta_4-\mathbf{\theta_3})(\theta_5-\mathbf{\theta_3})} e^{-\mathbf{\theta_3} \epsilon Z} \
&+ \frac{\theta_1 \theta_2 \theta_3 \theta_4}{(\theta_1-\mathbf{\theta_4})(\theta_2-\mathbf{\theta_4})(\theta_3-\mathbf{\theta_4})(\theta_5-\mathbf{\theta_4})} e^{-\mathbf{\theta_4} \epsilon Z} \
&+ \frac{\theta_1 \theta_2 \theta_3 \theta_4}{(\theta_1-\mathbf{\theta_5})(\theta_2-\mathbf{\theta_5})(\theta_3-\mathbf{\theta_5})(\theta_4-\mathbf{\theta_5})} e^{-\mathbf{\theta_5} \epsilon Z}
\end{aligned}
$$

> [!TIP] 脳内での作業フロー
> 1. **足し算の枠を5個書く**。
> 2. **後ろ（指数部分）**に $\theta_1 \dots \theta_5$ を順に書く。（ターゲット確定）
> 3. **上（分子）**に $\theta_1 \theta_2 \theta_3 \theta_4$ を全部書く。（共通）
> 4. **下（分母）**に $(\theta_{ALL} - \theta_{Target})$ を書く。


---

